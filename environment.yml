name: sre-agent
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - langchain>=0.3,<0.4
      - langchain-openai>=0.3,<0.4
      - langchain-community>=0.3,<0.4
      - chromadb>=0.6,<1.0
      - python-dotenv>=1.0,<2.0

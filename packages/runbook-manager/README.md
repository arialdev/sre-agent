# `@sre-agent/runbook-manager`

Next.js fullstack package responsible for runbook management, publication and
retrieval APIs.

## Commands

```bash
pnpm nx run runbook-manager:dev
pnpm nx run runbook-manager:build
pnpm nx run runbook-manager:typecheck
```

## Storage

Local development state is written to `storage/` inside this package.
